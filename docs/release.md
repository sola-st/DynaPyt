How to publish the latest version on this repository to PyPI:
0. Make sure that the main branch is passing the tests and is ready for publishing.
1. Update the version number both in `setup.py` and `src/dynapyt/__init__.py`.
2. Create a release on this repository. The appropriate GitHub action will publish the current version to PyPI.
